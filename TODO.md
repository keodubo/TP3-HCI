# TODO

- [ ] Integrate the Lists feature with the real backend repositories (replace `FakeListsRepository` and `FakeListDetailRepository`).
- [ ] Hook the “Nueva lista” dialog to the server create-list endpoint to persist user data.
- [ ] Replace the fake List Detail data source with the API once available (toggle, delete, add item, and share actions).
- [ ] Add deep link handling for `lists/{listId}` so external links open directly in the detail screen.

